# t323 记忆库质量读数：重复度 · 召回覆盖率 · 陈旧度

**结论：pass** —— 一张可判定的质量读数（全部 `mode=ro`，未写活库，未调 `/api/v1/recall`）。
**最值得优化的一件事（有依据）：`profile` 的近重复 —— 蒸馏路径把同一件事反复写成不同措辞。**
召回覆盖率与陈旧度**不足以判定**（第三态保留，理由见 ③④）。

## ① 规模与分布（活行 = `deleted_at IS NULL`）
| 项 | 读数 |
|---|---|
| 总行数 / 活行 / 墓碑 | 162 / **162** / **0** |
| superseded（`superseded_at` 非空） | **1** |
| `updated_at > created_at` 的行 | **1** |
| `source_episode` 为空 | **157 / 162** |
| `content_hash` 为空 | 0 |
| embedder | `fastembed:multilingual-e5-small`（162/162 已嵌入，384 维 / 1536 B） |

按 (store, namespace)（活行，12 组）：profile/user **44** · lesson/global **38** · observation/project:ruagent **33** · procedure/global **21** · observation/user **13** · procedure/project:ruagent **4** · observation/project:distill **3** · procedure/project:python-3.10.9-offline **2** · lesson/project:distill 1 · lesson/project:ruagent 1 · observation/project:qpl 1 · procedure/project:mouse-buttons-ahk 1 ⇒ **前四组占 136/162 = 84%**（高度集中）。
confidence 分布：0.8 ×81 · 0.9 ×45 · 1.0 ×25 · **0.5–0.7 ×11**。

## ② 近重复：三种度量 + 阈值 + 各自局限（不只给结论）
**度量 A（产品自己的口径）**：按 `memories.content_hash` 分组 ⇒ **>1 的组 = 0** ⇒ 无逐字重复。
**度量 B（我做的归一化）**：小写 + 折叠空白 + 去标点后完全相同 ⇒ **0 组**。
**度量 C（token Jaccard，同 store 内两两）**：Jaccard ≥0.5 ⇒ **2 对**（0.60：144/148 · 0.50：143/147）· ≥0.8 ⇒ **0 对**。
**度量 D（嵌入余弦，同 store 内两两，用库里已存的 embedding）**：≥0.90 ⇒ **736 对** · ≥0.95 ⇒ **44 对** · ≥0.99 ⇒ **2 对**。

### 度量 C/D 的局限（必须写明）
- **C 对中文几乎无效** ✗：token 化按空白切分，中文长句常变成**一整个 token** ⇒ 中文近重复**测不出来** ⇒ 这是它只报 2 对的原因，**不是**「没有重复」的证据。
- **D 的基线偏高** ✗：e5 系列的**无关文本余弦也在 0.7–0.85** 一带 ⇒ **0.90 不是重复的门槛**；本报告只把 **≥0.95 / ≥0.99** 当证据，0.90 那 736 对**不作为结论**。

### 度量 D 指向的簇（已逐条核对内容，不是只看分数）
| 余弦 | 行 | 内容（截断） | 判定 |
|---|---|---|---|
| 0.9968 | 123 / 126 | 「用户使用简体中文交流。」/「用户使用简体中文进行交流。」 | **同一事实** |
| 0.9904 | 117 / 142 | 「用户偏好使用简体中文交流。」/「偏好使用简体中文交流。」 | **同一事实** |
| 0.9893 | 117 / 123 | 同上两条 | **同一事实** |
| 0.9884 | 5 / 23 | 「…用 F6 作为启动/停止语音输入的快捷键」/「…以 F6 键作为…触发键」 | **同一事实** |
| 0.9898 | 143 / 147 | 「主力项目是 ruagent——一个本地优先…平台」/「用户的主力项目是 ruagent，一个本地优先…平台」 | **同一事实** |
⇒ **117/123/126/142 四行是同一件事的四种措辞**（created 09-19 → 09-25）· **5/23** 与 **143/147** 各是同一件事的两行 ⇒ **8/44 的 profile 行是冗余的（18%）**，且**全部集中在 `profile`/user**。

## ③ 召回覆盖率：**不足以判定**（第三态）
**用什么把 `recall_log` 与 memory 行对上** ✗：`recall_log` 的列是 `id, ts, query, strategy, top_n, memories, knowledge, wiki, entities, top_memory_score, top_knowledge_score, source` —— **只记计数，不记被召回的 memory id** ✗ ⇒ **逐行覆盖率无法从日志算出** ✗（我不用别的字段硬凑 ✗：query 文本与 memory 内容的字面匹配不能证明「那一行被召回过」✗）。
**能算的**：646 次召回事件中 **646 次 `memories > 0`**（每次都至少带回一条记忆）· **22 个不同 query** · 前两名 `kettle` **301** 次 · `autohotkey-v2` **295** 次 ⇒ **596/646 = 92% 是这两条**，而它们是**队里探针用的词** ✗ ⇒ **日志被测试流量主导** ✗。
⇒ **它不覆盖什么**：不覆盖「哪条记忆从未被召回」（无 id ✗）· 不覆盖「真实使用」（92% 是探针 ✗）⇒ **覆盖率这一项判定为不足以判定** ✓。

## ④ 陈旧度：**不足以判定**（第三态）
`created_at` 全部落在 **2026-09**（162/162）⇒ 库龄 **不足一个月** ⇒ **没有「旧」行可供判断** ✗。
近 N 天写入 / 召回：**7 天** 43 / 607 · **30 天** 162 / 646 · **90 天** 162 / 646 ⇒ **94% 的召回发生在最近 7 天**（同上：探针流量 ✗）· **7 天写入 43 行**（27% 的库在最近一周产生 ✓ 说明写入活跃）。
⇒ 陈旧度**不是「没有陈旧」，而是「现在测不出来」** ✓。

## ⑤ 判定：最值得优化的一件事
**把 `profile` 的近重复收掉 —— 让蒸馏在写入时就发现「这件事已经记过」。**
**依据**：① 8/44 的 profile 行是同一事实的重复措辞（②的逐条核对 ✓，不是只靠分数 ✓）；② 重复**全部**集中在 profile/user，而它正是注入层最常喂给模型的那一档（44 行里 0.8 以上置信度占多数 ✓）；③ **修法所需机制已经存在**：`supersedes` / `superseded_at` 列在位 ✓，而全库**只用过 1 次** ✗ ⇒ 缺的不是能力，是**写入时的近重复检查**（同 store+namespace 内、余弦 ≥ 约 0.95 ⇒ 走 supersede 而不是 insert ✓）。
**为什么不是别的**：度量 A/B/C 说**没有逐字重复**（不是问题 ✓）；墓碑 0、superseded 1 ⇒ 生命周期基本没用 ✓（与①同一根因 ✓）；**召回覆盖率与陈旧度不足以判定** ⇒ 不能据此排序 ✓。
**次要项（登记）**：`source_episode` **157/162 为空** ✗ ⇒ 记忆说不出来源 ⇒ 一旦要追溯「这条是谁写的、哪次 run 写的」就没有依据 ✓。

## ⑥ 方法与纪律
python 内置 `sqlite3`，**`file:…?mode=ro` + `uri=True`** ✓；**未写活库** ✓；**未调 `/api/v1/recall`** ✓（本单没有产生任何 recall_log 行 ✓）；只写 `docs/design/reviews/` ✓；未启停守护进程 ✓；未起浏览器 ✓。
三个探针脚本在 `C:/Users/19410/AppData/Local/Temp/sysprobe/t323mem{,2,3}.py` ✓（本单自带，收尾未留后台进程 ✓）。