# t267 独立验证 t265：形状守卫是否真的把空白清零（round 1）

**结论：needs_revision** —— t265 报的「10 格空白 → 0/10」**在我独立复跑的那 10 格上成立** ✓，
「零新增 UI」**成立** ✓（同格 DOM 与 500 那一列**逐项相同**），
**但守卫只校验「字段存在」，不校验「字段类型 / 数组元素形状」** ✗ ⇒ 我自造的三格**仍然整页空白** ✗
（作者未测过这三格），另有 **#memory 的错误文案把真实原因说反** ✗。

## 方法（不复用作者的脚本与读数）
- 自写探针（`node` 直调 `@playwright/test`，**无 npx** ✓）；**每格新开 page**（t264 的方法学更正 ✓）。
- 拦截方式：`page.route('**' + endpoint + '*')` ⇒ **必须带尾 `*`** ✗
  —— 我的第一版漏了它 ⇒ 6 源 × 4 变异**全部读成空白** ✗ ⇒ 那是**探针错，不是产品错** ✓（用单格对照诊断确认后重跑 ✓）。
- 读数 = `main.innerText` 字符数 · `h1` 数 · 可见错误元素数 · 可见空态数 · `pageerror` · 上屏文案 ✓。
- **BLANK 定义** = `chars === 0 || h1 === 0` ✓。

## ① 24 格矩阵（6 源 × 4 变异）—— 我自己的读数
| 源 | 缺字段 `{}` | 错形状 | 空数组 | 500 |
|---|---|---|---|---|
| memory/list | 146 / h1 1 / err 3 ✓ | 146 / 1 / 3 ✓ | **BLANK（PE `(t ?? []).filter is not a function`）** ✗ | 146 / 1 / 3 ✓ |
| knowledge/documents | 103 / 1 / 3 ✓ | 103 / 1 / 3 ✓ | 136 / 1 / 0（空态 ✓） | 103 / 1 / 3 ✓ |
| wiki/links | 100 / 1 / 3 ✓ | 100 / 1 / 3 ✓ | 388 / 1 / 0 ✓ | 100 / 1 / 3 ✓ |
| wiki/pages | 100 / 1 / 3 ✓ | 100 / 1 / 3 ✓ | 441 / 1 / 0 ✓ | 100 / 1 / 3 ✓ |
| graph/entities | 79 / 1 / 3 ✓ | 79 / 1 / 3 ✓ | 80 / 1 / 0 ✓ | 79 / 1 / 3 ✓ |
| graph/edges | 79 / 1 / 3 ✓ | 79 / 1 / 3 ✓ | 209 / 1 / 0 ✓ | 79 / 1 / 3 ✓ |

**改前 10/24 空白（t264）⇒ 改后 BLANK = 1/24** ✗（**不是我预期的 0**）：
**唯一残留 = `memory/list` × 空数组** ✗ —— 而那**正是 t265 的 10 格里没有的那一格** ✓（它测的是缺字段/错形状 ✓）。
⇒ **作者报「0/10」是真的** ✓（它只重测了那 10 格）；**但「24 格全清」不成立** ✗。

## ② 我自己构造的变异（作者未测）
| 变异 | memory | knowledge | wiki/pages | graph/entities | graph/edges |
|---|---|---|---|---|---|
| **数组元素形状错**（200，`[null]` 等） | 146 / 1 / 3 ✓ | **BLANK** ✗ `Cannot read properties of null (reading 'chunk_count')` | **BLANK** ✗ `… (reading 'stale')` | **208 / h1 1 / err 0 / empty 0** ✗（**静默**：看起来像空图） | 79 / 1 / 3 ✓ |
| **字段类型错**（`total: "54"` 字符串） | — | — | — | — | **209 / 1 / 0** ✗（**静默通过**） |

⇒ **守卫是「存在性」的，不是「类型性」的** ✗：`expectShape` 检查字段**在不在**，不检查**是不是那个类型 / 数组元素是不是对象** ⇒
**同一类失败（200 + 不合契约 ⇒ 空白或静默）仍然可达** ✗ —— 只是换了个入口。

## ③ 「零新增 UI」⇒ **成立** ✓
- **同格对比**：`memory` 的「200 + 缺字段」与「500」两格 **逐项相同** ✓（chars 146 · h1 1 · err 3 · 同一个 `.error-state` 类 · 同一段文案）✓；knowledge/wiki/entities 同理（103 / 100 / 79 与各自 500 列**完全一致**）✓。
- **守卫自己的消息不在界面上** ✓：DOM 文案里**没有** `malformed response` ✗（上屏的是视图自己的 `error-state` 文案 ✓）⇒ **没有新增 UI 元素** ✓。
- ⇒ 机制上就是「守卫抛错 ⇒ 与 5xx 走同一条 `catch` ⇒ 视图既有的错误态」✓（**这是设计意图成立的直接证据** ✓）。

## ④ 错误态文案 vs 真实原因（daemon 可达、200、不合契约）
| 页 | 上屏文案（实测） | 与真实原因 |
|---|---|---|
| **#memory** | 「无法读取记忆 **daemon 不可达**。读不到与「还没有记忆」是两件事。」 | **相反** ✗ —— daemon **可达** ✓ 且**返回 200** ✓ |
| **#graph** | 「无法读取图谱 **daemon 不可达或接口出错**。这不等于「图谱是空的」。」 | **基本相符** ✓（「或接口出错」覆盖了真实原因 ✓） |

⇒ **#memory 的文案会把用户引向「重启 daemon」** ✗（而真正该做的是「后端返回不合契约」✗）；
**#graph 的措辞是可接受的** ✓。端点与字段名只在抛出的 `Error` 上 ✗（t265 已如实披露 ✓）。

## ⑤ findings
1. **F-t267-01（high）**：守卫只查存在性 ⇒ **`memory/list` × 空数组**仍整页空白（`(t ?? []).filter is not a function`），**`knowledge/documents` 与 `wiki/pages` × 数组元素非对象**也整页空白。
2. **F-t267-02（medium）**：`graph/entities` × 数组元素非对象 ⇒ **208 字符、无错误、无空态** —— 页面**看起来是「空图」** ✗（静默的错数据比空白更难发现）。
3. **F-t267-03（medium）**：`graph/edges` × `total: "54"`（字符串）⇒ **静默通过** ✗（类型未校验）。
4. **F-t267-04（medium）**：`#memory` 的错误文案把原因说反（「daemon 不可达」而 daemon 可达、200）✗。
5. **F-t267-05（low）**：计数口径差 —— 我读 `err = 3`（`.error-state` + `.ant-alert-error` + `[role=alert]` 三者可重叠），作者读 2 ✓；两列**都不是错**，但报告里应写明计数口径 ✗。
