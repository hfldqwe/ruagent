//! ruagent-mock-agent: scriptable mock ACP agent.
//!
//! Implements an ACP server over stdio that replays scripted behavior
//! (tool calls, permission requests, crashes) for tests. See design §12.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_skeleton() {}
}
